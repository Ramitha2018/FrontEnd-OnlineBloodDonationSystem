/**import { TestBed, inject } from '@angular/core/testing';

import { \shared\userService } from './\shared\user.service';

describe('\shared\userService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [\shared\userService]
    });
  });

  it('should be created', inject([\shared\userService], (service: \shared\userService) => {
    expect(service).toBeTruthy();
  }));
});
*/
